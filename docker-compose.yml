version: '3'

services:
  postgres:
    image: postgres:alpine
    restart: always
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=insanefoam
      - POSTGRES_PASSWORD=qwerty
      - POSTGRES_DB=webchat
